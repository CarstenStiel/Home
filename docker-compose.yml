version: "3.7"
services:
  frontend:
    container_name: zuhause-frontend
    build: .
    volumes:
      - "./:/app"
    ports:
      - "3000:8080"
    networks:
      - network_1
    command: npm run serve

networks:
  network_1:
